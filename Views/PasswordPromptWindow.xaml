<Window x:Class="OpenClawManager.Views.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password"
        Height="230" Width="420"
        MinHeight="210" MinWidth="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="BtnOk"
                    Width="90" Height="28" Margin="4,0"
                    IsDefault="True" />
            <Button x:Name="BtnCancel"
                    Width="90" Height="28" Margin="4,0"
                    IsCancel="True" />
        </StackPanel>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="TxtMessage"
                       Grid.Row="0"
                       TextWrapping="Wrap"
                       Margin="0,0,0,10" />

            <TextBlock x:Name="TxtPasswordLabel"
                       Grid.Row="1"
                       Margin="0,0,0,4" />
            <PasswordBox x:Name="PwdPassword"
                         Grid.Row="2"
                         Height="24"
                         Margin="0,0,0,10" />

            <TextBlock x:Name="TxtConfirmLabel"
                       Grid.Row="3"
                       Margin="0,0,0,4" />
            <PasswordBox x:Name="PwdConfirm"
                         Grid.Row="4"
                         Height="24" />
        </Grid>
    </DockPanel>
</Window>
