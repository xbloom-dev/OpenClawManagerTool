<Window x:Class="OpenClawManager.Views.TokenEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Token"
        Height="250" Width="520"
        MinHeight="230" MinWidth="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="BtnSave" Content="Save"
                    Width="100" Height="28" Margin="4,0"
                    IsDefault="True" />
            <Button x:Name="BtnCancel" Content="Cancel"
                    Width="100" Height="28" Margin="4,0"
                    IsCancel="True" />
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="TxtIdLabel" Grid.Row="0" Grid.Column="0"
                       Text="ID:" VerticalAlignment="Center"
                       Margin="0,0,8,10" />
            <TextBox x:Name="TxtId" Grid.Row="0" Grid.Column="1"
                     Height="24" VerticalContentAlignment="Center"
                     Margin="0,0,0,10" />

            <TextBlock x:Name="TxtValueLabel" Grid.Row="1" Grid.Column="0"
                       Text="Value:" VerticalAlignment="Center"
                       Margin="0,0,8,10" />
            <PasswordBox x:Name="PwdValue" Grid.Row="1" Grid.Column="1"
                         Height="24"
                         Margin="0,0,0,10" />

            <TextBlock x:Name="TxtDescriptionLabel" Grid.Row="2" Grid.Column="0"
                       Text="Description:" VerticalAlignment="Center"
                       Margin="0,0,8,4" />
            <TextBox x:Name="TxtDescription" Grid.Row="2" Grid.Column="1"
                     Height="24" VerticalContentAlignment="Center"
                     Margin="0,0,0,4" />

            <TextBlock x:Name="TxtHint" Grid.Row="3" Grid.Column="1"
                       Text="ID may contain letters, digits and underscore. Value must be at least 8 characters."
                       TextWrapping="Wrap"
                       Foreground="Gray"
                       FontSize="11"
                       Margin="0,4,0,0" />
        </Grid>
    </DockPanel>
</Window>
